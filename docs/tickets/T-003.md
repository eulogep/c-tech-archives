# T-003 — Utilisateur personnalisé et rôles

```text
TICKET:
T-003 — Utilisateur personnalisé et rôles

STATUS:
DONE

BRANCH:
feature/custom-user-roles

COMMIT:
feat(accounts): add custom user and roles

PRE_IMPLEMENTATION_CHECK:
- 18 migrations Django initiales étaient appliquées, dont les migrations auth utilisant le modèle User par défaut.
- auth_user existait mais contenait 0 utilisateur ; auth_group, django_admin_log et django_session contenaient également 0 donnée.
- Les 3 dépendances vers auth_user provenaient des tables techniques Django (groupes, permissions et journal d’administration), sans application métier ni donnée utile.
- La base PostgreSQL locale a donc été jugée réinitialisable sans perte utile.

IMPLEMENTATION:
- Custom User : accounts.User basé sur AbstractUser.
- AUTH_USER_MODEL : configuré sur accounts.User.
- Rôles : ADMINISTRATEUR, AGENT_ARCHIVES et CONSULTANT centralisés par Role (TextChoices).
- Django Admin : UserAdmin adapté avec colonnes, filtres et recherche pertinents.
- Migrations : accounts.0001_initial générée, inspectée et appliquée.

DATABASE:
- migrations appliquées : migrations Django techniques et accounts.0001_initial.
- réinitialisation : la seule base locale c_tech_archives a été supprimée et recréée après vérification des données vides.
- justification : éviter une migration de transition de auth.User vers accounts.User alors que le projet ne contenait ni données métier ni dépendances fonctionnelles.
- schéma vérifié : accounts_user présent, auth_user absent et contrainte accounts_user_role_is_valid présente.

FILES_CHANGED:
- accounts/admin.py
- accounts/models.py
- accounts/tests.py
- accounts/migrations/0001_initial.py
- config/settings.py
- tests/test_project.py
- docs/architecture.md
- docs/database.md
- docs/security.md
- docs/tests.md
- docs/decisions.md
- docs/environment.md
- docs/tickets/T-003.md
- README.md

TESTS:
- nombre : 14.
- validations principales : création d’utilisateur, hachage et vérification de mot de passe, trois rôles métier, refus de rôle invalide, activation/désactivation, AUTH_USER_MODEL, superutilisateur et non-régression PostgreSQL.

TEST_RESULT:
- python manage.py check : succès, 0 problème.
- python manage.py makemigrations --check --dry-run : succès, aucune migration manquante.
- python manage.py migrate --noinput : succès, aucune migration restante.
- python manage.py test : succès, 14 tests.

SECURITY:
- password hashing : assuré par le gestionnaire et les mécanismes Django ; aucun mot de passe en clair dans les données applicatives.
- séparation rôle/superuser : ADMINISTRATEUR métier n’accorde ni is_staff ni is_superuser.
- secrets : aucun credential n’est versionné ; .env reste ignoré par Git.
- permissions préparées : le champ role est centralisé, tandis que groupes et permissions Django restent disponibles pour les contrôles serveur futurs.

DOCUMENTATION:
- architecture, base de données, sécurité, décisions, environnement et README mis à jour.

DECISIONS:
- AbstractUser est retenu afin de préserver les mécanismes Django et de rester simple à expliquer.
- username est conservé comme identifiant technique du MVP ; email est obligatoire et unique.
- toutes les futures relations utilisateur utiliseront settings.AUTH_USER_MODEL ou get_user_model().

LIMITATIONS:
- aucun écran de connexion, logout, récupération de mot de passe, CRUD utilisateur, archive, audit, API ou RBAC métier complet n’est inclus dans ce ticket.
- la règle de privilège CREATEDB du compte local c_tech_app est limitée à l’environnement de test ; elle ne doit pas être reprise en production.

RISKS:
- la politique exacte d’identification par username ou email devra être réévaluée si C-Tech fournit une exigence d’authentification différente.
- les droits fins par archive doivent être définis dans les tickets fonctionnels avec des permissions serveur explicites.

NEXT_TICKET:
T-004 — Modèles métier / structure des archives

GIT_COMMIT:
feat(accounts): add custom user and roles
```
